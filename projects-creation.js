function projectCreation(name, a) {
    let oneProjectTime = 3;
    let totalHours = oneProjectTime * a;
    let totalProjects = a;
    console.log(`The architect ${name} will need ${totalHours} hours to complete ${totalProjects} project/s.`);
    
}

projectCreation("George", 4);